trigger:
- master

pool:
  vmImage: 'Ubuntu 16.04'

steps:
- script: |
    set -e
    GOOS=linux scripts/setup/dev_setup
    make lint
    make build
    make binaries
    make test
  displayName: 'Build'